#include <stdio.h>

/**
 * print_array - print elements of array
 *
 * @a: the array
 * @b: the lenght of a
 *
 * Return: nothing
 */

void print_array(int *a, int n)
{
	int i;

	for (i = 0 ; i < n ; i++)
	{
		printf("%d, ", a[i]);

	}
}

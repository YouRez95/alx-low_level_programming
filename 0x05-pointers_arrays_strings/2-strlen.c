#include <stdio.h>
#include <string.h>
/**
 * _strlen - function return the lenght of string
 *
 * @s: param
 *
 * Return: the length
 */
int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}

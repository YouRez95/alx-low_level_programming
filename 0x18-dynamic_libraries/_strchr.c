#include <string.h>
/**
 * _strchr - locates a character in a string
 *
 * @s: string
 * @c: character
 *
 * Return: pointer in the string
 */

char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
